#
# ~/.bash_extras
#

i3-setup.sh "urxvt" ${i3LINES[*]}

#TODO: discover why I need to manually pass the path to my xinitrc
if [ -z "${DISPLAY}" ] && [ "${XDG_VTNR}" -eq 1 ]; then
  exec startx $HOME/.xinitrc
fi
