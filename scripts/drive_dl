#!/usr/bin/env python3

import os
import sys
import drive_handling

SCOPES = ['https://www.googleapis.com/auth/drive']
start_dir = '/home/dromansk/Documents'
os.chdir(start_dir)
if os.path.exists('instruments') == False:
	os.mkdir('instruments')
os.chdir('instruments')

def user_input():
	print("Enter name of folder to download")
	dl_fold = input()
	return dl_fold

if len(sys.argv) < 2:
	dl_set = [user_input()]
else:
	dl_set = sys.argv[1:]
drive = drive_handling.drive_setup(SCOPES)
for item in dl_set:
	pwd = os.getcwd()
	if os.path.exists(item) == False:
		os.mkdir(item)
	os.chdir(item)
	drive_handling.dl_items(drive, item)
	os.chdir(pwd)
